class Slick {
    constructor(element, slideToShow) {
        this.slider = element;
        this.slideCount = this.slider.length;
        this.slideToShow = slideToShow
    }

    animate() {

    }
}